import parseFlows from './flows-parser';

const parseWorkflows = (parsedFlows, workflows, splitters, parsedWorkflowsUntilNow = []) => {
    const parsedWorkflows = workflows.map(workflow=>{
      const parsedWorkflow = parsedFlows()
    })
};

export default parseWorkflows;
