const createUser = customParams => customParams;
const removeUser = customParams => customParams;
const updateServer = customParams => customParams;
const getUser = customParams => customParams;

export default {createUser, removeUser, updateServer, getUser};