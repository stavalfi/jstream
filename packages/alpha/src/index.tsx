import React from "react";
import ReactDOM from "react-dom";
import Main from '@alpha/main'
import '@alpha/matrix-styles.css'

const rootElement = document.getElementById('app')
ReactDOM.render(
  <Main />,
  rootElement,
)
