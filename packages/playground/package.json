{
  "name": "@jstream/playground",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "yarn run exec b:start",
    "build:dev": "yarn run exec b:build:dev",
    "build:prod": "yarn run exec b:build:prod",
    "test": "yarn run exec b:test",
    "test:unit": "yarn run exec b:test:unit",
    "test:watch": "yarn run exec b:test:watch",
    "pre-hook-stage": "FOLDER=playground NOT_IDE=true MANUAL_RUN=true yarn lint-staged --config ./lint-staged.config.js",
    "clean": "rm -rf dist",
    "clean:all": "yarn run clean && rm -rf node_modules",
    "exec": "FOLDER=playground WEB_APP=true yarn workspace @jstream/build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@jstream/editor": "^1.0.0",
    "@jstream/flower": "^1.0.0",
    "@jstream/parser": "^1.0.0"
  },
  "devDependencies": {
    "@jstream/build": "^1.0.0"
  }
}
